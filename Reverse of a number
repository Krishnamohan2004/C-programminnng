#include <stdio.h>
void main()
{
    int rev,n,rem;
    scanf("%d",&n);
    rev=0;
    while(n>0)
    {
        rem=n%10;
        rev=rev*10+rem;
        n=n/10;
        
    }
    printf("the reverse number %d",rev);
}
