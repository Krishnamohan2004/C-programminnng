#include <stdio.h>
void main()
{
    int a[20],i,n;
    printf("enter the no of size");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        printf("enter the no of elements");
        scanf("%d",&a[i]);
    }
    for(i=n-1;i>=0;i--)
    {
        printf("%d\n",a[i]);
    }
    }

